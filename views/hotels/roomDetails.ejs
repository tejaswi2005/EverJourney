<%- include('../layouts/boilerplate', { title: room.type }) %>

<div class="container my-5">
  <div class="row">
    <div class="col-md-6">
      <img src="<%= room.image_url %>" class="img-fluid rounded shadow" alt="<%= room.type %>">
    </div>
    <div class="col-md-6">
      <h2><%= room.type %></h2>
      <p><strong>Price:</strong> ₹<%= room.price_per_night %>/night</p>
      <p><strong>Occupancy:</strong> <%= room.occupancy %> guests</p>
      <p><strong>Amenities:</strong> <%= room.amenities %></p>
      <button class="btn btn-success w-100 mt-3">Book Now</button>
    </div>
  </div>
</div>
